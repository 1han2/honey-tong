# Antigravity 작업 히스토리 및 Handover 가이드

본 문서는 다른 PC에서 Antigravity 에이전트와 작업을 이어서 할 때, 작업 내역과 상태를 빠르게 파악할 수 있도록 작성된 가이드 문서입니다.

---

## 📅 세션 주요 변경 및 해결 내역

### 1. 자막 및 타이틀 스타일 최적화 (Aesthetics)
* **나레이션 자막 5안 (파스텔 옐로우) 반영**: 나레이션 자막 박스의 배경색을 옅고 부드러운 파스텔 노란색 (`#FFF59D`)으로 지정했습니다. 연예인의 실제 대사 자막(흰색 `#FFFFFF`)과 화면상에서 시각적으로 뚜렷하게 대비됩니다.
* **타이틀 짤림 현상 해결**: 상단 훅 타이틀(`HookTitle`) 텍스트가 좌우로 짤리던 현상을 해결하기 위해 폰트 크기 자동 계산 스케일 계수를 `0.65`에서 `0.72`로 조정하고 최소 폰트 크기를 `90`으로 낮추어, 꽉 차면서도 화면 경계 밖으로 이탈하지 않도록 교정했습니다.
* **직각 자막 박스**: 자막 박스의 `borderRadius`를 `0`으로 처리하고 `6px` 두께의 블랙 테두리를 적용했습니다.

### 2. 말 끊김 현상 및 영상의 기승전결(맥락) 개선
* **FFmpeg 물리적 컷팅 마진 버퍼 추가 (`src/services/media-production-service.ts`)**:
  - AI가 대본 타임스탬프를 대략적으로 유추하여 잘라낼 때 말꼬리가 뚝 잘려 나가는 것을 방지하고자, 컷팅 시점에 **앞으로 0.3초 여유 시작 (`-300ms`), 뒤로 0.8초 여유 종료 (`+800ms`)** 마진 버퍼를 소스코드 수준에서 강제 적용했습니다.
* **프롬프트 룰 강화 (`base_prompt/make_transcript.md` 및 `src/lib/prompts.ts`)**:
  - **나레이션 명사형 종결 절대 금지 (초강력 규칙)**: 나레이션이 감자칩, 원픽 등 명사로 뚝 끊기며 허무하게 끝나지 않고, "~라는데", "~나 봄" 등 구어체 서술형 종결어미를 갖추도록 규칙과 올바른 예시를 보강했습니다.
  - **여운 마무리 (결 - 초강력 결말 규칙)**: 전체 스크립트 흐름이 제품 설명 도중 뚝 끝나지 않고, 영상 마지막에 반드시 출연자의 인사나 털털한 웃음소리, 위트 있는 다짐 등의 마무리 리액션 클립(source_clip)으로 매듭을 짓도록 강제했습니다.

### 3. Supertone AI TTS 복구
* **원인**: Cloud Run Job 배포 시에 `SUPERTONE_API_KEY`와 `SUPERTONE_VOICE_ID` 환경 변수 주입이 빠져 있어서, API 키 결핍으로 인해 오디오 렌더러가 구글 Cloud TTS로 계속 자동 우회(fallback)하고 있었습니다.
* **해결**: `scripts/deploy.sh`에서 로컬 `.env`에 정의된 Supertone 키와 보이스 ID를 자동으로 긁어내서 Cloud Run 환경 변수로 올바르게 밀어 넣어주도록 스크립트를 보완했습니다.

### 4. 도커 이미지 빌드 & 원격 캐시 버그 해결
* **원인**: Cloud Build에 적용해 둔 buildx 레지스트리 캐시(`mode=max`)가 소스코드 수정 사항(자막 스타일 변경)을 탐지하지 못하고 캐싱된 이전 레이어를 그대로 배포해버려 실서버에 미반영되는 현상이 발생했습니다.
* **해결**: `cloudbuild.yaml`에서 오동작하던 레지스트리 캐시 옵션을 제거하고 순수 Docker Directory Hashing 기반의 BuildKit 빌드로 전면 교정하여, 코드 수정 사항이 확실히 실서버 이미지에 탑재되게 고쳤습니다.

---

## 🏃‍♂️ 다음 작업 단계 (Next Actions)

1. **실서버 렌더링 동작 확인**:
   - `scripts/deploy.sh`를 실행하여 모든 변경 사항이 실서버에 정상 반영되었음을 확인했습니다.
   - 텔레그램 봇이나 API를 통해 새 영상을 하나 생성하여 **1) 노란색 나레이션 자막**, **2) 깔끔한 타이틀 오토핏**, **3) 말이 끊기지 않는 컷팅 마진**, **4) 고품질 Supertone TTS 음성**이 맥락에 맞게 올바르게 적용되는지 모니터링합니다.
2. **2단계 단어 단위 정밀 얼라인먼트 (선택적 고도화)**:
   - 만약 단순 마진 추가(+800ms)로도 영상 컷이 여전히 완벽하지 않고 불만족스럽다면, 넉넉히 자른 임시 오디오로부터 각 단어별 정확한 밀리초 단위 발화점을 STT JSON Schema를 통해 추적해 2차로 잘라내는 'Word-level forced alignment' 개발에 착수합니다.
